# Keyboard Controller container Python dependencies
#
# The controller is a ROS-free TCP client (it sends JSON to ros_bridge), so it
# needs no rclpy. Only pynput is required, and only for the optional native
# "pynput" input mode; the container path uses stdin (standard library only).
# On Linux, pynput pulls python-xlib automatically.

pynput==1.7.6
